export default function SettingsPage() {
    return (
        <div className="flex items-center justify-center h-full text-text-secondary">
            Configuration Page (Pending Implementation)
        </div>
    );
}
